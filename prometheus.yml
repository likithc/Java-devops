global:
  scrape_interval: 15s # How often to scrape data

scrape_configs:
  - job_name: 'employee-app'
    metrics_path: '/actuator/prometheus'
    static_configs:
      - targets: ['[YOUR_EC2_PUBLIC_IP]:8081'] # Point this to your active Blue container
